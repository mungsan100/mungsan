// generated 타입(모델·enum·Prisma 클라이언트 내부)은 전부 `DB` 네임스페이스 경유로만 노출한다 —
// `DB.CripEditStatus`(enum)·`DB.Crip`(model)·`DB.Prisma.sql`(클라이언트 내부). 최상위 평면 재노출(`export *`)은
// 일부러 두지 않는다: 앱 로컬 동명 파생 타입과의 충돌을 막고, generated 타입 참조를 한 네임스페이스로 강제한다.
// schema 변경 시 자동 반영(수동 목록 불필요).
export * as DB from '../generated/client';
export { prisma } from './prisma-client';
export { sortKeyBetween, type SortKeyErr } from './sort-key/sort-key';
export { parseDescriptionBlocks, type DescriptionBlock } from './description-block/description-block';
