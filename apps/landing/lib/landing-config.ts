/* ==========================================================================
 * 랜딩페이지 설정 — 나중에 바꿔야 할 값은 전부 이 파일 한 곳에 모여 있습니다.
 * 페이지 코드(app/page.tsx)는 손대지 말고, 아래 값만 수정하세요.
 * ========================================================================== */

/* --------------------------------------------------------------------------
 * ⚠️ [교체 필요 1] 회원가입 버튼 링크
 * --------------------------------------------------------------------------
 * 지금은 임시로 '#' 입니다. MVP 회원가입 주소가 확정되면 아래 한 줄만 바꾸세요.
 * 예)  export const SIGNUP_URL = 'https://mungsan2-nnm0.somsatang.cloud/signup';
 *
 * 이 값 하나가 히어로 '시작하기' 버튼과 최종 CTA '지금 시작하기' 버튼에
 * 동시에 적용됩니다. 두 군데를 따로 고칠 필요 없습니다.
 * ------------------------------------------------------------------------ */
export const SIGNUP_URL = '#';

/* --------------------------------------------------------------------------
 * ⚠️ [교체 필요 2] 앱 스크린샷 이미지
 * --------------------------------------------------------------------------
 * src가 null이면 회색 placeholder 박스가 표시됩니다.
 * 실제 스크린샷으로 교체하려면:
 *   1) 이미지 파일을 apps/landing/public/screenshots/ 폴더에 넣고
 *   2) 아래 src를 '/screenshots/파일이름.png' 로 바꾸면 끝입니다.
 *
 * 예)  src: '/screenshots/lounge.png',
 *
 * label은 placeholder 박스 안에 표시되는 안내 문구이고,
 * ratio는 박스의 가로세로 비율입니다(교체 후에도 그대로 유지됩니다).
 * ------------------------------------------------------------------------ */
export type Screenshot = {
  src: string | null;
  alt: string;
  label: string;
  /** 바깥 박스의 가로세로 비율 */
  ratio: string;
  /**
   * 이미지를 어떤 모양으로 넣을지.
   * - 'phone': 세로로 긴 앱 스크린샷을 가로 박스 가운데에 폰 모양으로 얹는다.
   *   실제 앱(apps/service)이 480px 폭 모바일 셸이라 스크린샷이 세로로 길기 때문에,
   *   가로 박스에 그대로 늘리면 찌그러진다. 기본값.
   * - 'wide': 가로형 그래픽을 박스에 꽉 채운다.
   */
  mode?: 'phone' | 'wide';
};

export const SCREENSHOTS = {
  /* 5. 작동 원리 */
  howItWorks: {
    src: null,
    alt: '뭉산 파트너 추천 화면',
    label: '파트너 추천 / 협업 마켓플레이스 화면',
    ratio: '16 / 10',
  },
  /* 6. 임원 전용 라운지 — 섹션 내 크게 배치 */
  lounge: {
    src: null,
    alt: '뭉산 임원 전용 익명 라운지 화면',
    label: '임원 전용 익명 라운지 앱 화면',
    ratio: '16 / 10',
  },
  /* 7. 협업 마켓플레이스 — 섹션 내 크게 배치 */
  marketplace: {
    src: null,
    alt: '뭉산 협업 마켓플레이스 화면',
    label: '협업 마켓플레이스 앱 화면',
    ratio: '16 / 10',
  },
  /* 8. My 셰르파 — Beta Preview 라벨 표시 */
  sherpa: {
    src: null,
    alt: '뭉산 공동 수행 관리 대시보드 화면',
    label: '공유 대시보드 / 공동 수행 관리 화면',
    ratio: '16 / 10',
  },
  /* 9. 데이터 기반 분석 — 짙은 녹색 배경 위에 올라갑니다 */
  dataGraphic: {
    src: null,
    alt: '데이터 기반 파트너 추천 구조 그래픽',
    label: '데이터 기반 파트너 추천 구조 그래픽',
    ratio: '16 / 9',
    // 앱 화면이 아니라 가로형 그래픽을 넣을 자리라 'wide'로 둔다.
    mode: 'wide',
  },
} satisfies Record<string, Screenshot>;
