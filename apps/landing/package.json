{
  "name": "@mungsan/landing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev -p 4200",
    "build": "next build",
    "//start": "배포(솜사탕클라우드)는 컨테이너가 $PORT에 바인딩되기를 요구한다. -p 플래그는 PORT 환경변수보다 우선하므로 절대 붙이지 말 것 — 붙이면 플랫폼이 헬스체크를 못 해 rollout timeout으로 배포가 실패한다. next start는 PORT가 있으면 그 값을, 없으면 3000을 쓴다.",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "next": "16.2.6",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@mungsan/eslint": "*",
    "@mungsan/prettier": "*",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
